import React from "react";

export type SearchProps = {
  setShowSearch?: React.Dispatch<React.SetStateAction<boolean>>;
  showSearch?: boolean;
};

export type StylesSearchProps = {
  showSearch?: boolean;
};
